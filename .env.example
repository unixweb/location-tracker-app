# Authentication
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-key-here

# NextAuth URL
# Development
NEXTAUTH_URL=http://localhost:3000
# Production (change to your domain)
# NEXTAUTH_URL=https://your-domain.com

# n8n API (optional - currently using client-side fetch)
N8N_API_URL=https://n8n.unixweb.home64.de/webhook/location

# Database (for future use)
# DATABASE_URL=postgresql://user:password@localhost:5432/location_tracker

# Email (for notifications - future feature)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Push Notifications (future feature)
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
